{
  "name": "my-project",
  "version": "1.0.0",
  "description": "Пример проекта с CI/CD",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "build": "echo 'Building project...' && mkdir -p dist && touch dist/build.txt",
    "test": "echo 'Running tests...' && exit 0"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "jest": "^29.0.0"
  },
  "engines": {
    "node": ">=16"
  }
}
